# This file is included to match the requested file structure.
# In a pure client-side application, a Python configuration file is not used.
# If this project were to be expanded with a Python-based backend,
# this file would be used to store configuration variables such as database credentials, secret keys, etc.

class Config:
    DEBUG = True
    SECRET_KEY = 'a-secret-key'
